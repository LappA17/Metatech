/* Сюда собранно уже всё что попало: callbackify, promisify, format(для форматирование строк), deprecate, types(для проверки типов) то-есть всё что используется в разных местах Нодовоскго приложения

*/
